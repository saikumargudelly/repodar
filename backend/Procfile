web: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
worker: celery -A app.celery_worker worker --loglevel=info --concurrency=2 -Q ingestion,scoring
beat: celery -A app.celery_worker beat --loglevel=info

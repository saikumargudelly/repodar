/**
 * NinjaRankPill — DEPRECATED shim.
 * All callers should migrate to: import { StatusDot } from "@/components/ui/StatusDot"
 * This re-export is retained for backwards-compatibility during the transition period.
 */
export { StatusDot as NinjaRankPill } from "@/components/ui/StatusDot";
